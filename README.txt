Codes for the miniproject in Software and Automation Frameworks (ROB5) by Bjarke Stück

This project contains the files:
Client.py 
Server-py 
The file "processing_time_tables.csv" handed out for the project
And a text documment named "decoded_info.txt" containing the data sent between the server and client
To run the python codes, create a new project in Pycharm, the copy-paste the four files into the folder, and install the libraries "numpy" and "pandas"

The "PLC.project" is a project made for controlling the PCL and is made in CodeSys.
